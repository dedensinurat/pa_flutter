import 'package:flutter/material.dart';

class RevisiDPPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Revisi DPP")),
      body: Center(child: Text("Isi dari Revisi DPP")),
    );
  }
}
