import 'package:flutter/material.dart';

class LogbookFinalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Logbook Final")),
      body: Center(child: Text("Isi dari Logbook Final")),
    );
  }
}
